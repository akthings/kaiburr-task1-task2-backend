# MongoDB connection details (will be injected via Kubernetes Environment Variables)
# In local dev, you would typically set these to localhost:27017
# Example for a Kubernetes/Docker deployment:
spring.data.mongodb.host=${MONGO_HOST}
spring.data.mongodb.port=${MONGO_PORT}
spring.data.mongodb.database=${MONGO_DATABASE}